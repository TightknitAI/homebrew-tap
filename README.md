# Tightknit Homebrew Tap

Homebrew formulae for [Tightknit](https://tightknit.ai) CLI tools.

## Usage

```bash
brew tap tightknitai/tap
brew install tightknitai/tap/tightknit
```

## Status

The Tightknit CLI is currently in alpha. Install via npm for now:

```bash
npx @tightknitai/cli@alpha
```
